import React from 'react';
import './PWAUpdater.css';

export const PWAUpdater: React.FC = () => {
    // Temporarily disabled to prevent infinite update loops
    // The app will still work as a PWA, just without update notifications
    return null;
};
